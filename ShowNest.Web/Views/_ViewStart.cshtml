@inject _LoggedInLayoutService loggedInLayoutService

@{
    var result = await loggedInLayoutService.GetHeaderViewInfo();

    if (!Context.User.Identity.IsAuthenticated || result == null)
    {
        Layout = "_UnLoggedInLayout";
    }
    else
    {
        Layout = "_LoggedInLayout";
    }
}
s