@* @model System.Collections.Generic.Dictionary<string, string>
 *@
@model EcpayClientComformViewModel

@section topCSS {
    <link href="~/css/account/ecpayview.css" rel="stylesheet" />
}


<!--step6 : 顯示回傳結果-->
<div class="width section_wrap context mt-4 mb-4">
    <div style="display:none;" > 
       @*  @foreach (var key in Model.Keys.ToList())
        {
            @(key) <input type="text" name="@key" value="@Model[key]" disabled />

            <br />
        } *@
    </div>
    <h2 >付款完成</h2>
    <p>訂單編號: #@Model.Id </p>
    <p>您的付款已經成功處理,我們感謝您的購買。</p>
   
    <a class="btn btn-primary mb-4 mt-4" onclick="navigateToOrderDetail()">確認</a>
</div>
<script>
    function navigateToOrderDetail() {
        window.location.href = '/Events/OrderDetail?id=' + @Model.Id;
    }
</script>

@* href="@Url.Action("OrderDetail", "Events", new { id = Model.Id })">確認 
asp-controller="Events" asp-action="OrderDetail" asp-route-id="@Model.Id"*@